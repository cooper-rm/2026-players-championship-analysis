# Copy to .env and fill in your key, then load it into your shell:
#   export $(grep -v '^#' .env | xargs)
# .env is git-ignored — your key never gets committed.
# Get your key at datagolf.com -> API access (requires a Scratch Plus subscription).
DATAGOLF_API_KEY=your_key_here
