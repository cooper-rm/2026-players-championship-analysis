[tool.pytest.ini_options]
# Put the repo root on sys.path so tests can `from src.scrape_pgatour import ...`
pythonpath = ["."]
# Only look for tests in tests/ (faster, no surprises)
testpaths = ["tests"]
